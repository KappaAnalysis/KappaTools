#------------------------------------------------------------------------------
PROGRAM  = libKToolbox.so libKToolbox.a
LIBFILES = libKToolbox.o ProgressMonitor.o StringTools.o
#------------------------------------------------------------------------------

include ../Makefile.base
COPTS += -pedantic
