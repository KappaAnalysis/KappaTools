#------------------------------------------------------------------------------
PROGRAM  = libToolbox.so libToolbox.a
LIBFILES = toolbox.o ProgressMonitor.o IOHelper.o StringTools.o
#------------------------------------------------------------------------------

include ../Makefile.raw
COPTS += -pedantic
